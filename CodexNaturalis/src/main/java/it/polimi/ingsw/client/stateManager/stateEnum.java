package it.polimi.ingsw.client.stateManager;

public enum stateEnum {
    LOGIN,
    SELECT_NUM_PLAYERS,
    LOBBY,
    SELECT_TOKEN,
    WAITING_TURN,
    PLAY_CARD,
    DRAW_CARD,
    DISCONNECTION,
    ALREADY_STARTED
}
